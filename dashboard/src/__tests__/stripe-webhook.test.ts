/**
 * stripe-webhook.test.ts — REMOVED
 *
 * Stripe billing was removed when the project went open-source.
 * This file is kept as a placeholder to avoid breaking the test runner
 * glob pattern. No assertions — the test suite passes trivially.
 */

describe('stripe-webhook (removed)', () => {
  it('Stripe billing removed — open-source project uses self-serve token provisioning', () => {
    expect(true).toBe(true)
  })
})
