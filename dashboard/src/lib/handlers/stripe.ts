/**
 * handlers/stripe.ts — DISABLED
 *
 * Stripe billing was removed when the project went open-source.
 * This module is intentionally empty. Token provisioning is handled
 * via admin seed scripts (dashboard/scripts/seed-test-token.mjs).
 */

export {}
